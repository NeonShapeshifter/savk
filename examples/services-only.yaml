apiVersion: savk/v1
kind: ApplianceContract
metadata:
  name: services-only
  target: linux-systemd

services:
  sensor-agent.service:
    state: active
    run_as:
      user: sensor
      group: sensor
    restart: on-failure
    capabilities:
      - CAP_NET_BIND_SERVICE
