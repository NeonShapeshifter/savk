apiVersion: savk/v1
kind: ApplianceContract
metadata:
  name: inline-comment
  target: linux-systemd

services:
  sensor-agent.service:
    state: active
    capabilities:
      - CAP_NET_BIND_SERVICE # inline comments are unsupported
